/**
 * Created by ari on 11/20/2015.
 *
 * This will eventually become a Relay tool for loading and playing games in the Relay Client
 */

if(typeof module === 'object') (function() {
    module.exports.initClientAppMusicCommands = function (Client) {

        // TODO: add proxy commands for loading games in the Relay client

    };
})();